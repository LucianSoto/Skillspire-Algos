var searchInsert = function(nums, target) {
  // let x = target - 1
  // if (target > nums.length) {
  //   x = nums.length - 1
  // }

  // for( let i = 0; i < nums.length; i++ ) {
  //   console.log(nums[x], target, x )
  //   // if nums[x] === x - 2 return x+1
  //   // if nums[x] === x + 2 return x-1
  //   if ( nums[x] > target ) {
  //     x--
  //   } else if ( nums[i] < target ) {
  //     x++
  //   } else if(nums[x] === x - 2) {return x + 1, '1'}
  //     else if(nums[x] === x + 2) {return x - 1, '2'}
  //     else {
  //     // console.log(x)
  //     return x + ' result'
  //   }
  // }

  
};

console.log(searchInsert([1, 3, 4, 5], 2))